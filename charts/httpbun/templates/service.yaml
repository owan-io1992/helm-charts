apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpbun.fullname" . }}
  labels:
    {{- include "httpbun.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httpbun.selectorLabels" . | nindent 4 }}
